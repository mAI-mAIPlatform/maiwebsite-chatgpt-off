const d=new Date();document.getElementById('year')&&(document.getElementById('year').textContent=d.getFullYear());const c=document.getElementById('bg');if(c){const x=c.getContext('2d');let w,h,dots=[];function R(){w=c.width=innerWidth;h=c.height=innerHeight;if(!dots.length){dots=Array.from({length:24},()=>({x:Math.random()*w,y:Math.random()*h,r:40+Math.random()*120,vx:(Math.random()-.5)*.4,vy:(Math.random()-.5)*.4,a:.08+Math.random()*.12}))}}addEventListener('resize',R);R();(function L(){x.clearRect(0,0,w,h);for(const d of dots){d.x+=d.vx;d.y+=d.vy;if(d.x<-200||d.x>w+200)d.vx*=-1;if(d.y<-200||d.y>h+200)d.vy*=-1;const g=x.createRadialGradient(d.x,d.y,0,d.x,d.y,d.r);g.addColorStop(0,`rgba(255,255,255,${d.a})`);g.addColorStop(1,'rgba(255,255,255,0)');x.fillStyle=g;x.beginPath();x.arc(d.x,d.y,d.r,0,Math.PI*2);x.fill()}requestAnimationFrame(L)})()}